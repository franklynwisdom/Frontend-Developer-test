import styled from "styled-components";

const StyledArtWrapper = styled.section`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(25rem, 1fr));
  /* justify-content: center; */
  grid-gap:1.5rem;
  place-items: center;

`;

export default StyledArtWrapper;
