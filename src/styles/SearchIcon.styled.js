import { FaSistrix } from "react-icons/fa";
import styled from "styled-components";

const StyledSearchIcon = styled(FaSistrix)`
  position: relative;
  left: -20.5rem;
  top: 0.3rem;
  color:#FBAF00;
  font-size: 20px;

;
`;

export default StyledSearchIcon;
